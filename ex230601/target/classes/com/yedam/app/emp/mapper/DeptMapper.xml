<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.emp.mapper.DeptMapper">
	<select id="deptList">
		SELECT *
		FROM departments
		ORDER BY department_name
	</select>
	<select id="selectDept">
		SELECT department_id, department_name, manager_id, location_id
		FROM departments
		WHERE department_id = #{departmentId}
	</select>
	
	<select id="selectManagers">
		SELECT employee_id, first_name
		FROM employees
		ORDER BY employee_id
	</select>
	<select id="selectLocations">
		SELECT LOCATION_ID, STREET_ADDRESS, CITY
		FROM locations
		ORDER BY location_id
	</select>
	
	<insert id="insertDept">
		INSERT INTO departments(department_id, department_name, manager_id, location_id)
		VALUES (departments_seq.nextval, #{departmentName}, #{managerId}, #{locationId} )
	</insert>
	<update id="updateDept">
		UPDATE departments
		SET department_id=#{departmentId}, department_name=#{departmentName}, manager_id=#{managerId}, location_id=#{locationId}
	</update>
	
</mapper>