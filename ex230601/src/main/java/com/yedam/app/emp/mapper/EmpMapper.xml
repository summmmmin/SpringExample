<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.emp.mapper.EmpMapper">
	<insert id="insertOne" parameterType="empVO">
		INSERT INTO employees(EMPLOYEE_ID, FIRST_NAME, LAST_NAME, EMAIL, HIRE_DATE, JOB_ID)
		VALUES (EMPLOYEES_SEQ.nextval , #{firstName} , #{lastName}, #{email} , sysdate , 'IT_PROG') 
	</insert>
	<update id="updateOne">
		UPDATE employees
		SET first_name = #{firstName}
		WHERE employee_id = #{employeeId}
	</update>
	<select id="selectList" resultType="empVO">
	  SELECT *
		FROM employees
		<where>
		<if test="departmentId != null and departmentId != '' ">
		 and department_id = #{departmentId}		
		</if>
		<if test="firstName != null and firstName != '' ">
		 and first_name like '%' || #{firstName} || '%'		
		</if>
		</where>
		ORDER BY employee_id
	</select>
	<select id="selectOne" parameterType="int" resultType="com.yedam.app.emp.service.EmpVO">
		SELECT employee_id,
			   first_name,
			   last_name
		FROM employees
		WHERE employee_id = #{empNo}
	</select>
</mapper>  
